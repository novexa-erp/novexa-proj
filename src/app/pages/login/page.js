import LoginPage from "../../components/LoginPage";

export const metadata = {
  title: "Sign In — Novexa ERP",
  description: "Sign in to your Novexa ERP account.",
};

export default function Login() {
  return <LoginPage />;
}
