Feature implementation completed successfully. Summary:

INVENTORY (User Side):
✅ Added costPrice and sellingPrice fields in product creation form
✅ costPrice field: labeled as 'Cost Price (Rs.)'
✅ sellingPrice field: labeled as 'Selling Price (Rs.)'
✅ Both fields are required when creating simple products
✅ Both are saved to Firebase
✅ User can see both fields when creating/editing products
✅ Product card displays sellingPrice instead of old price field
✅ Backward compatibility maintained (price = sellingPrice)

ADMIN PANEL (Super Admin Side):
✅ Product price hidden - shows '••••••' instead
✅ Variant prices hidden - shows '••••••' instead
✅ Trash view product prices hidden - shows '••••••' instead
✅ Super admin CANNOT see cost or selling prices
✅ Product name, stock, and other fields remain visible

FIREBASE DATA STRUCTURE:
{
  name: 'Product Name',
  costPrice: 1000,       // Only user can see
  sellingPrice: 1500,    // Only user can see
  price: 1500,           // Kept for backward compatibility
  stock: 100,
  variantType: 'none',
  // ... other fields
}

All changes implemented successfully!
